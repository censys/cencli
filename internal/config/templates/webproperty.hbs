{{#each this}}
Hostname: {{orange hostname}}:{{orange port}}

Labels:
{{#each labels}}
  - {{blue value}} (confidence {{yellow confidence}})
{{/each}}

Software:
{{#each software}}
  - {{blue vendor}} {{blue product}} (confidence {{yellow confidence}})
{{/each}}


Endpoints ({{blue (length endpoints)}}):
{{#each endpoints}}
- IP: {{blue ip}}
  Status: {{blue http.status_code}} {{blue http.status_reason}}{{#if http.headers.Location.headers.[0]}} → {{blue http.headers.Location.headers.[0]}}{{/if}}
  Server: {{blue http.headers.Server.headers.[0]}}
  Scan Time: {{blue scan_time}}

{{/each}}



------------------------------------------------------------
{{/each}}


TODO if the product starts with the vendor, drop the vendor
TODO drop confidence
TODO make labels and software have the header with length
TODO show endpoint type in the list